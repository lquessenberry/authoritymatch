langcode: en
status: true
dependencies:
  config:
    - field.storage.node.field_physical_address
    - node.type.authority
  module:
    - address
id: node.authority.field_physical_address
field_name: field_physical_address
entity_type: node
bundle: authority
label: 'Physical Address'
description: 'Primary operating address'
required: true
translatable: false
default_value: {  }
default_value_callback: ''
settings:
  available_countries:
    US: US
  langcode_override: ''
  field_overrides:
    givenName:
      override: hidden
    additionalName:
      override: hidden
    familyName:
      override: hidden
    organization:
      override: hidden
    addressLine1:
      override: optional
    addressLine2:
      override: optional
    postalCode:
      override: optional
    locality:
      override: optional
    administrativeArea:
      override: optional
  fields: {  }
field_type: address
